# Coords are stored directly in scoreboards via /trigger
# This function is no longer called from tick - coords persist in scoreboards
# The teleport function reads f1sch.tp_x/y/z when f1sch.tp is triggered
